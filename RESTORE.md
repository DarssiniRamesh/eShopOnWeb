# eShopOnWeb Restore Diagnostics

Date: [Generated by CI during restore]
Command:
- `dotnet restore eShopOnWeb.sln -v diag /p:RestoreForce=true /bl:restore.binlog`

Environment:
- .NET SDK: 8.0.119
- OS: Linux (container/CI)
- DOTNET_SKIP_FIRST_TIME_EXPERIENCE=1, DOTNET_NOLOGO=1

Outcome:
- Restore: SUCCESS (no errors)
- Sources: Default NuGet sources reachable; signatures validated
- Platform: Packages compatible with net8.0 targets; no OS-specific restrictions encountered
- Conflicts: None detected
- Artifacts: `restore.log`, `restore.binlog` (in repo root if executed locally)

Security advisories (observed in restore output):
- System.Text.Json 8.0.3 — High severity (GHSA-8g4q-xg66-9fp4, GHSA-hh2w-p6rv-4g7w)
- Azure.Identity 1.10.4 — Moderate severity (per NuGet advisories)

Recommended actions:
1) Bump vulnerable dependencies in `Directory.Packages.props`:
   - System.Text.Json: upgrade to latest patched 8.0.x or a compatible newer major version.
   - Azure.Identity: upgrade to latest 1.11.x or higher compatible version.
2) Consider enabling NuGet package auditing in CI to fail on High severity advisories.
3) Keep EF Core packages aligned across the solution to avoid runtime mismatches.

Notes:
- No .NET SDK mismatch detected with global.json/installed SDKs during this restore.
- No NuGet.config issues observed at repo root; if a custom feed is needed, add/verify `NuGet.config`.
