Test Reports for eShopOnWeb

The following HTML report artifacts were generated by the test run:

1) Test Results (HTML summary):
   Path: reports/TestResults/index.html

2) Code Coverage (HTML summary):
   Path: reports/TestCoverage/summary.html

Notes:
- Source TRX and coverage files are under eShopOnWeb/TestResults/.
- If some integration tests require external services (e.g., SQL Server) and are unavailable, the failures will be reflected directly in the HTML report.
- No .cs-based reports were created.
